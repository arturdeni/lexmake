---
// src/components/Home/HomeClosing.astro
---

<section class="home-closing">
  <div class="home-closing__container">
    <div class="home-closing__content">
      <p class="home-closing__text">
        Quiero ayudar a personas y empresas a tener la imagen que se merecen, a
        mostrar al mundo qué saben hacer.
        <span class="home-closing__text--italic"
          >Si has llegado hasta aquí es que tienes algo que contarme.</span
        >
      </p>

      <div class="home-closing__cta">
        <a href="/contact" class="normal-link">Contactar</a>
      </div>
    </div>
  </div>
</section>

<style>
  .home-closing {
    background-color: var(--color-grey);
    position: relative;
    z-index: 15;
    padding: var(--spacing-xxxl) 0;
    margin: var(--spacing-xxxl) 0;
  }

  .home-closing__container {
    max-width: 1000px;
    margin: 0 auto;
    padding: 0 var(--container-padding);
  }

  /* === CONTENT LAYOUT === */
  .home-closing__content {
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    gap: var(--spacing-xxl);
  }

  /* === TEXT === */
  .home-closing__text {
    font-family: var(--font-hanken);
    font-size: var(--h4-size);
    font-weight: 500;
    line-height: var(--h4-line-height);
    margin: 0;
    max-width: 700px;
    color: var(--color-black);
  }

  .home-closing__text--italic {
    font-family: var(--font-gallery);
    font-style: italic;
    font-weight: 400;
  }

  /* === CTA === */
  .home-closing__cta {
    text-transform: uppercase;
  }

  /* === RESPONSIVE === */
  @media (max-width: 1024px) {
    .home-closing__text {
      font-size: calc(var(--h3-size) * 0.9);
    }
  }

  @media (max-width: 768px) {
    .home-closing {
      padding: var(--spacing-xl) 0;
    }

    .home-closing__content {
      gap: var(--spacing-xl);
    }

    .home-closing__text {
      font-size: calc(var(--h3-size) * 0.75);
    }
  }

  /* === PREPARAR PARA ANIMACIONES GSAP === */
  /* Por defecto los elementos son visibles */
  /* GSAP los ocultará antes de animar con gsap.set() */
</style>

<script>
  // Importar GSAP y ScrollTrigger
  import gsap from "gsap";
  import { ScrollTrigger } from "gsap/ScrollTrigger";

  // Registrar el plugin ScrollTrigger
  gsap.registerPlugin(ScrollTrigger);

  // Función para inicializar las animaciones
  const initHomeClosingAnimations = () => {
    // Verificar que el elemento existe
    const homeClosing = document.querySelector(".home-closing");
    if (!homeClosing) return;

    // Establecer estado inicial de los elementos (ocultos)
    gsap.set(".home-closing__text", { opacity: 0, y: 30 });
    gsap.set(".home-closing__cta .normal-link", { opacity: 0, y: 20 });

    // Animar el texto principal
    gsap.to(".home-closing__text", {
      opacity: 1,
      y: 0,
      duration: 0.9,
      ease: "power2.out",
      scrollTrigger: {
        trigger: ".home-closing__text",
        start: "top 80%",
        toggleActions: "play none none none",
      },
    });

    // Animar el enlace
    gsap.to(".home-closing__cta .normal-link", {
      opacity: 1,
      y: 0,
      duration: 0.7,
      delay: 0.3,
      ease: "power2.out",
      scrollTrigger: {
        trigger: ".home-closing__cta",
        start: "top 80%",
        toggleActions: "play none none none",
      },
    });

    console.log("HomeClosing animations initialized");
  };

  // Ejecutar cuando el DOM esté listo
  if (document.readyState === "loading") {
    document.addEventListener("DOMContentLoaded", initHomeClosingAnimations);
  } else {
    initHomeClosingAnimations();
  }
</script>
